services:
  braid:
    build: .
    image: braid:latest
    ports:
      - "8080:8080"
    volumes:
      - ./configs:/app/configs:ro
      - ./artifacts:/app/artifacts
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
